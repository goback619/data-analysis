<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.witown.location.mapper.WiLocationMapper">

    <select id="get" resultType="com.witown.location.entity.WiLocation">
        SELECT * FROM wi_location WHERE id =#{id}
    </select>

</mapper>
